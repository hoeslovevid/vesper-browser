# GitHub Actions

These workflows were inherited from Zen Browser. They publish Zen artifacts and use Zen secrets.

Do **not** enable them on a Vesper GitHub repo until you replace them. A push would otherwise try to build and ship as Zen.

Until then, build locally with `npm run build` as described in `docs/vesper/BUILDING.md`.
